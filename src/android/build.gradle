dependencies {

    compile 'com.android.support:support-core-utils:26.+'
    /************* SyncNow SDK management **************/
    // use the SyncNow SDK as external lib
	compile(name:'libSyncNowDetector', ext:'aar')
}